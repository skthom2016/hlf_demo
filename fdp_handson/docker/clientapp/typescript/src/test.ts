import { Order } from './order';
import { Po } from './po';
main();

// Sets up the gateway | executes the invoke & query
async function main(){
    try {
        let poId:string;
       
            poId = process.argv[2];
        console.log(poId);
        // 2. Setup the gateway object

    } catch(error){
        
    }
}
